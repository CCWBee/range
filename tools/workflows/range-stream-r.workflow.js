export const meta = {
  name: 'range-v2-stream-r',
  description: 'Implement RANGE v2 stream R (modular three.js renderer, camera, cursor and HUD, scene, effects, bundler) against the finished P and B streams, then review through three lenses, refute and fix',
  phases: [
    { title: 'R implement', detail: 'src/*, index.html, build.py, shots.ps1' },
    { title: 'R review', detail: 'contract, regression, design lenses, refuted' },
    { title: 'R fix', detail: 'apply survivors, tests and build green, five stage shots' },
  ],
}
const ROOT = 'E:/claude-projects/range'
const SPEC = ROOT + '/docs/specs/2026-09-06-range-v2.md'
const TRAILER = 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01Hxhee5WfJforgYFHEYjaA1'
const HOUSE = `House rules for every file you write: British English, no em dashes anywhere (restructure the sentence, or use a comma, colon or full stop), plain comments that say what and why. No pill or badge labels, no decorative dots, no emoji as icons, no rule lines along headings; every HUD element is an instrument. Report facts with evidence (the command, its exit code, the numbers, the screenshot path), never a claim that something works on the strength of an edit. Work only inside ${ROOT}. Do not touch physics.js, control.js, tools/test_flight.mjs, assets/*, textures/*, the Blender scripts, concepts/* or the existing screenshots except the five stage renders you produce.`
const READ_FIRST = `Read in full before writing anything: ${SPEC} (the spec), ${ROOT}/CLAUDE.md, ${ROOT}/docs/notes/stream-p.md and ${ROOT}/docs/notes/stream-b.md (what the other two streams actually delivered and any deviation from the spec), then ${ROOT}/physics.js and ${ROOT}/control.js (the interface you consume), ${ROOT}/assets/meshes.json (the manifest: asset names, pivots, materials, terrain), ${ROOT}/tools/export_meshes.py (how the binary is laid out), ${ROOT}/game.js and ${ROOT}/index.html (the renderer you replace: keep what is good in it, in particular the sky, ocean, concrete and flame shaders, the bloom, the audio and the staging), ${ROOT}/tools/build.py, and the concept frames ${ROOT}/concepts/*.png (Read them; they are the target look). Design decisions in the spec are settled; where the notes record a deviation, the notes win over the spec text.`
const CHECK = `cd ${ROOT} && node tools/test_flight.mjs && python tools/build.py`
const SHOTS = `Headless renders of the five stages (from PowerShell; the tool is documented in ${ROOT}/CLAUDE.md, Run): node E:\\claude-projects\\design\\tools\\qa\\shot.mjs --url E:\\claude-projects\\range\\dist\\index.html --out E:\\claude-projects\\range\\screenshots\\<file>.png --width 1920 --height 1080 --wait 3000 --eval "window.range.stage('<stage>')" for ramp -> ramp.png, takeoff -> takeoff.png, cloud -> cloud-break.png, bomb -> bomb-run.png, landing -> landing.png; add --eval "window.range.stage('ramp'); JSON.stringify(window.range.metrics())" to read draw calls and triangles. Read every PNG you produce and judge it against its concept frame honestly. Headless frame times are meaningless; the session measures fps in Brave.`
const FINDINGS = { type: 'object', properties: { findings: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['must-fix', 'should-fix', 'note'] }, file: { type: 'string' }, summary: { type: 'string' }, scenario: { type: 'string' }, fix: { type: 'string' } }, required: ['severity', 'file', 'summary', 'scenario', 'fix'] } } }, required: ['findings'] }
const VERDICT = { type: 'object', properties: { confirmed: { type: 'boolean' }, reason: { type: 'string' }, revisedFix: { type: 'string' } }, required: ['confirmed', 'reason'] }
const A = (prompt, label, phase, extra = {}) => agent(prompt, { label, phase, model: 'fable', effort: 'high', ...extra })

phase('R implement')
const impl = await A(`You implement stream R of the RANGE v2 spec: the renderer. ${READ_FIRST} ${HOUSE}

You own exactly: ${ROOT}/src/*.js (new), ${ROOT}/index.html, ${ROOT}/tools/build.py, ${ROOT}/tools/shots.ps1 (new), ${ROOT}/docs/notes/stream-r.md, and the five stage renders under ${ROOT}/screenshots/. game.js is retired once src/main.js runs (leave the file in place, index.html simply stops loading it; the session deletes it after review).

Build sections 1.1 (placement coordinates, pavement rectangles via the physics export), 5 (all of it: modules and bundling rules, camera, cursor and pointer lock and pause semantics, keys and help text and hints, world with lighting, two shadow maps, instanced scenery at the listed coordinates, instanced lights with the billboard glow shader, clutter, terrain shader with the analytic coast discard, clouds, aircraft assembly from the pivots with animated surfaces, gear, doors, nozzles, lights, condensation, spray, tyre smoke, craters, HUD with the three markers and the new fields, staging and metrics with stress and slowFraction), the R items of 6, and the budget of 7. The loader must survive a missing texture (flat colour fallback) and a missing asset (log and skip), because the library may still be moving.

Method: get the dev page running over http (python -m http.server 8080 in ${ROOT}, run in the background) and the bundle building early, then build up module by module, checking each in a headless render. Verify with ${CHECK} (exit 0, every test PASS, bundle bytes printed and under 25 MB) and with ${SHOTS}. Read your five renders against the concept frames and fix what does not read before returning: the ramp must show the skinned aircraft with drooped canards, slab seams and puddles without a sun disc, hangars, tower, lights and grass; the takeoff plumes and spray; the cloud-break clouds and coast; the bomb-run pads and a falling bomb; the landing PAPI and approach lights. Report draw calls and triangles per stage from metrics().

Do not commit. Final report (data for the next agents): the module list with one line each, the exact bundling rule you implemented, the key map, the metrics per stage, the bundle byte count, the screenshot paths, every deviation from the spec with its reason, and anything left unfinished.`, 'R:implement', 'R implement')

phase('R review')
const ctx = `Stream R of the RANGE v2 spec (${SPEC}; sections 1.1, 5, 6, 7, 8) has been implemented in ${ROOT}/src/*.js, index.html, tools/build.py and tools/shots.ps1 against the finished physics.js, control.js and assets library (read ${ROOT}/docs/notes/stream-p.md and stream-b.md for what those deliver). The implementer reported:\n\n${impl}\n\nRead the spec sections and the files in full. The check command is: ${CHECK}. ${SHOTS}`
const lenses = [
  { key: 'contract', prompt: `READ-ONLY review, edit nothing outside ${ROOT}/tools/scratch/ and the screenshots you take under ${ROOT}/screenshots/review/. ${ctx}\n\nYour lens: contract. Walk sections 1.1, 5, 6, 7 and 8 clause by clause and report what is missing, half done, done differently without a recorded reason, or added beyond the spec. Check the interface consumption too: every field of flight and instructor the spec names is read from the right place with the right units and signs (the aim direction is the camera ray through the cursor, the three markers sit where 5.3 says, surfaces drive the right parts about the manifest axes), and the bundling rules of 5.1 are the ones implemented (open dist/index.html headless and confirm it runs with the CSP in place). Each finding carries a scenario and the smallest fix.` },
  { key: 'regression', prompt: `READ-ONLY review, edit nothing outside ${ROOT}/tools/scratch/ and the screenshots you take under ${ROOT}/screenshots/review/. ${ctx}\n\nYour lens: regression and runtime behaviour. Run the check command yourself and report its real output. Then exercise the running page headless with --eval scripts: stage each of the five poses and read metrics() (draw calls within 160, triangles within 2.0 M, no console errors: read them with the tool's console output), call window.range.stress(true) and confirm the render targets resize (canvas.width doubles and metrics().pixelRatio reports 2), drop a bomb from the bomb pose and step 10 s (window.range.dropBomb then advance with several frames) and confirm a wreck and a crater appear, fire the gun from the bomb pose at a target, restart with reset() and confirm the scene is clean (no leftover bombs, wrecks or craters), toggle instructor mode, and drive the aircraft through a staged take-off using setAim and the keys object for 30 s of simulated time to see that nothing throws. Look for NaN in the HUD fields at V = 0 on the ramp. Each finding carries the numbers or the console line and the smallest fix.` },
  { key: 'design', prompt: `READ-ONLY review, edit nothing outside ${ROOT}/tools/scratch/ and the screenshots you take under ${ROOT}/screenshots/review/. ${ctx}\n\nYour lens: design and anti-slop, judged from renders, not from code. Take the five stage renders yourself at 1920x1080 and Read each beside its concept frame in ${ROOT}/concepts/. Default to refuting the claim that a frame matches: for each item in section 8 say whether it reads in the render, with the pixel evidence. Then hold the HUD, help panel, hints and crash overlay to the house bar: British English, no em dashes, no pill or badge labels, no decorative dots, no emoji, no rule lines along headings, one dominant element, copy that says what it means. Report what fails as findings with the smallest fix; do not report taste as a defect unless the spec or the house bar names it.` },
]
const reviews = await parallel(lenses.map(l => () => A(l.prompt, `R:review:${l.key}`, 'R review', { schema: FINDINGS }).then(r => ({ lens: l.key, findings: r?.findings || [] }))))
const live = reviews.filter(Boolean).flatMap(r => r.findings.filter(f => f.severity !== 'note').map(f => ({ ...f, lens: r.lens })))
const verdicts = await parallel(live.map(f => () => A(`READ-ONLY, edit nothing outside ${ROOT}/tools/scratch/. ${ctx}\n\nA reviewer (${f.lens} lens) filed this finding:\nFile: ${f.file}\nSeverity: ${f.severity}\nSummary: ${f.summary}\nScenario: ${f.scenario}\nProposed fix: ${f.fix}\n\nTry to refute it by reading the actual code and, where it is about behaviour or a render, by reproducing the scenario yourself (headless renders and --eval scripts are fine). It is confirmed only if the scenario really produces the wrong outcome and the fix is right, or you state a better one in revisedFix. Default to confirmed=false when uncertain and say why.`, `verify:${f.lens}:${f.summary}`.slice(0, 60), 'R review', { schema: VERDICT }).then(v => ({ ...f, verdict: v }))))
const survivors = verdicts.filter(Boolean).filter(x => x.verdict?.confirmed)
log(`R: ${survivors.length} confirmed findings of ${live.length}`)

phase('R fix')
const fix = await A(`You fix stream R of the RANGE v2 spec. ${READ_FIRST} ${HOUSE} Files you own: ${ROOT}/src/*.js, index.html, tools/build.py, tools/shots.ps1, docs/notes/stream-r.md, the five stage renders under screenshots/.\n\nImplementer's report:\n${impl}\n\nConfirmed findings to apply (skip one only with a written reason):\n${JSON.stringify(survivors, null, 1)}\n\nApply them, re-run ${CHECK} until it passes, re-take the five stage renders (${SHOTS}) and Read them, update docs/notes/stream-r.md, then commit only the stream R files with git (user.name "Charles Bee", user.email "641charlesbee@gmail.com"), message "Stream R: modular renderer, chase camera, mouse-aim cursor and HUD, grounded scene with lights and clutter, single-file bundle" followed by a blank line and these trailers exactly:\n${TRAILER}\n\nFinal report: the check output, metrics per stage, bundle bytes, screenshot paths, what you applied, what you skipped and why.`, 'R:fix', 'R fix')
return { survivors, fix }
